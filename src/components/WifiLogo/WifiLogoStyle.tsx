import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title:{
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 30,
    color: '#CCC'

  },
  image: {
    resizeMode: 'contain',
    height: 200,
    width: 350
  }
});