import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  textInput: {
    width: '100%',
    backgroundColor: '#CCC',
    borderRadius: 5,
    padding: 10,
    color: 'black',
    fontSize: 20,
    height: 40,
    borderWidth: 1,
    textAlign: 'center',
    marginBottom: 10
  }
});